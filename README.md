# Spotlight Launcher

A simple spotlight-style launcher for Windows.

Open apps or websites instantly from a command bar.

## Installation

Clone the repo:

git clone https://github.com/username/spotlight-launcher

Install dependencies:

pip install -r requirements.txt

Run the launcher:

python launcher.py

## Configure commands

Edit commands.json to add your own commands.